import TimerView from "@/components/application/Timer/TimerView";
import { DefaultLayout } from "@/layouts/DefaultLayout";
import React from "react";

const Timer = () => {
    return <DefaultLayout>
        <TimerView />
    </DefaultLayout>;
};

export default Timer;
